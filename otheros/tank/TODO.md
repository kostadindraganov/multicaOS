# tank — TODO

- [ ] Smoke-test workflow before deploy (curl /health, import sanity, etc.)
- [ ] Per-project permission mode override (some projects ask, others bypass)
- [ ] Add Codex alongside Claude Code in the task runtime
- [ ] WebSocket reader leak — verify the fix actually held under sustained PTY load
- [ ] Surface project's git status (clean / dirty / behind) on the project home
- [ ] More git providers for PR/merge (github, gitlab)
- [ ] Auto-archive completed chats after N days (with snapshot of transcripts)
