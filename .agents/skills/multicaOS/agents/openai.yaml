interface:
  display_name: "MulticaOS"
  short_description: "Repo-specific patterns and workflows for multicaOS"
  default_prompt: "Use the multicaOS repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true